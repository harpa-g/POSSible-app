import { authHeader } from './auth.service';

/* TODO 5-2: Implement function to place an order on checkout.
   Send a POST request to /api/orders/checkout with authHeader.
   The body should contain { items, totalPrice }.
   Then go to AdminPage.jsx */
export async function placeOrderApi(cart) {

}

/* TODO 6-2: Implement function to fetch all orders for admin.
   Send a GET request to /api/orders with authHeader. Then go to AdminPage.jsx */
export async function getOrdersApi() {
}